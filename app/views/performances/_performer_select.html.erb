<%= select_tag "performance[performer_id]",
    options_from_collection_for_select(
        performers,
        :id,
        :display_name,
        selected_performer
        ),
        include_blank: true,
        data: {
          controller: "searchable-select",
          searchable_select_allow_empty_value: false,
          searchable_select_placeholder_value: "選択してください"
        },
        class: "w-full" %>
